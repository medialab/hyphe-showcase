{
  "name": "vitrine_final",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "node ./build.js",
    "serve": "npm run build && cd build && http-server -o -p 8000",
    "live": "nodemon server.js",
    "dev-server": "sleep 2 && cd build && http-server -o -p 8000",
    "dev": "concurrently 'npm run live' 'npm run dev-server' --kill-others"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.0.0",
    "fs-extra": "^8.1.0",
    "handlebars": "^4.5.3",
    "http-server": "^0.11.1",
    "marked": "^0.7.0"
  },
  "dependencies": {
    "hbs": "^4.0.6",
    "jquery": "^3.4.1",
    "material-design-lite": "^1.3.0",
    "yaml-front-matter": "^4.0.0"
  }
}
