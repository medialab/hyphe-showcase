{
  "name": "vitrine",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "node ./build.js",
    "serve": "npm run build && cd build && http-server -o -p 8000",
    "watch": "node ./watch.js",
    "dev-server": "sleep 2 && cd build && http-server -o -p 8000",
    "dev": "concurrently 'npm run watch' 'npm run dev-server' --kill-others"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "http-server": "^0.11.1",
    "jstransformer-handlebars": "^1.1.0",
    "metalsmith-discover-partials": "^0.1.2",
    "metalsmith-layouts": "^2.3.1",
    "metalsmith-markdown": "^1.3.0"
  },
  "dependencies": {
    "metalsmith-watch": "^1.0.3"
  }
}
